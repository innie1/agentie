SUPABASE_URL=https://cugaysbdpfzunwwlbfsn.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

CREDENTIALS_ENCRYPTION_KEY=   # must match server's key exactly, they decrypt the same data

OPENROUTER_API_KEY=your_openrouter_key_here
# Optional. When set to a 1536-dimension embedding model, memory retrieval uses vectors.
# Without it, memory safely falls back to lexical retrieval.
OPENROUTER_EMBEDDING_MODEL=

REDIS_URL=redis://default:password@host:port   # Railway provisions this automatically if you add a Redis service

WORKER_PORT=4100
